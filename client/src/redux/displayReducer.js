import * as actionTypes from "./actionTypes";
import boardHelpers from "./displayHelpers";

export default function displayReducer(state = [], action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
